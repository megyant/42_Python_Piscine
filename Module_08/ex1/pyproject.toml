[tool.poetry]
name = "loading"
version = "1.0.0"
description = "Data analysis"
authors = ["mbotelho <mbotelho@student.42lisboa.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
numpy = "^2.2.6"
matplotlib = "^3.7.2"
requests = "^2.31.0"
pandas = "^2.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"